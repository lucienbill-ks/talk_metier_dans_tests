to ouvrir_chrome myUrl
	log "ouvrir_chrome" && myUrl
	TypeText windowsKey, "r"
	wait 0.5
	typeText "chrome.exe" && myUrl && "--start-maximized" && "--incognito", returnKey
	wait 1
	WaitFor 30.0, "01_monolithe/chrome_icone_chargement"
	logSuccess "ouvrir Chrome OK"
end ouvrir_chrome